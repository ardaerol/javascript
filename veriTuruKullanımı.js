let price = 100
let tax = 0.18
let priceTax = price*tax
let total = price + priceTax
console.log(
   "Fiyat", price,
    "KDV oranı", tax,
    "KDV tutarıp", priceTax,
    "Yeni fiyat", total
    )

    let counter = 0
    counter = counter+1

    // Boolean veri turu

    let isActive = false
    isActive = true
    console.log(isActive)

    let userName = "arda"
    let isUserName = Boolean(userName)
    console.log(isUserName)